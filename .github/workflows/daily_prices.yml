name: Daily Price Refresh

on:
  schedule:
    - cron: "0 17 * * 1-5"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Create virtual environment & install dependencies
        run: |
          python -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install --no-cache-dir supabase "yfinance==1.4.0" "websockets>=13.0" python-dotenv pandas plotly google-generativeai requests textblob
          .venv/bin/python -c "import nltk; nltk.download('punkt'); nltk.download('averaged_perceptron_tagger')"

      - name: Run price refresh
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GNEWS_KEY: ${{ secrets.GNEWS_KEY }}
        run: .venv/bin/python update_prices.py
